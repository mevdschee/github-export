# test/fixtures

This directory contains files added by TEST PRs as fixtures for the exporter
end-to-end test suite (`e2e_test.go`).

The contents are not used by the exporter itself — only the existence of these
PRs (and their merge state) matters to the test suite.
